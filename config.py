# choose how to extend the path for saving files (remote = absolute, for cronjob)
# the shared variable is set when the program is first run
path_extension = ""
path_extensions = {
  "local": "./",
  "remote": "/root/album-bot/"
}